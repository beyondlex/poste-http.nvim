-- dev/config.lua — Poste configuration
--
-- Return a table of opts passed to require("poste").setup(opts).
-- Edit freely, no re-setup needed — loaded live on every nvim start.
--
-- Available options: see lua/poste/init.lua M.setup()
--
-- Examples:
--   return {
--     keymaps = {
--       sql = {
--         execute = { "<C-e>", mode = "n" },
--       },
--     },
--     import_chunk_size = 500,
--   }

return {
}
