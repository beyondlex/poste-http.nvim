# 代码复核报告 — 2026-09-08

范围：基线全量测试 + 核心管线精读（run.lua / response_parser.lua /
curl_exec.lua / executors/{init,graphql}.lua / env.lua / resolve.lua）+
全库 Lua 陷阱横向扫描（gsub 计数泄漏、and-or 布尔坍缩、空分支、未使用
绑定、shadowing）+ 文档与命令清单比对。基线 **1438 用例 / 102 spec 文件**
全绿，复核后保持全绿，lua/ 的 luacheck 警告 88 → 1（语义上有意的
first-match-only 循环，已加注释）。

## 结论

未发现新的功能性问题——多轮 review 的痕迹明显（错误路径 pcall、busy
清除、exactly-once 回调、凭证打码都已到位）。本次改动以清洁与文档为主。

## 已提交

1. **websocket spec 的空断言**：`assert.is_nil(closed)` 读的是未声明全局
   （恒为 nil），"stdin 保持打开"的保证从未被真正断言。stub 现在捕获
   `chanclose` 调用并断言为零次。
2. **lua/ luacheck 清理**：
   - 空白行 / 未使用局部绑定（多返回值解构改为 `_`）；
   - 三处"空 then / 逻辑全在 else"反转（cache.find_request_line、
     context_detector 注释补全分支、request_deps pending 跳过）；
   - `describe_content` 的死赋值（file/opts 参数保留并注明仅为调用点兼容）；
   - import_openapi / import_swagger `build_url` 里从未参与结果的主机拼接
     （URL 刻意换成 `{{base_url}}`，现已注释说明）；
   - `request_deps` 导入文件块界改名为 `import_start/import_end`，不再
     遮蔽目标块的 `block_start/block_end`（遮蔽变量曾与外层同名，易误读）。
3. **doc/poste.txt 增补**：GRAPHQL / GRPC / WEBSOCKET / SCRIPT 请求行方法
   （此前 help 只列到普通 HTTP 动词）与 `:PosteHttpCmpStatus` /
   `:PosteHttpCmpProfile`。多协议细节指向 docs/user/syntax.md。

## 记录在案（不确定 / 有意保留）

- **tests/ 仍有 ~27 条 luacheck 警告**（spec 内 upvalue 遮蔽、未使用变量）。
  均为测试内部风格问题，无行为影响；家族其他仓库已清零，http 体量大，
  建议单独一次纯清理提交。
- **`curl_exec` 用 shell 字符串 + `shell_escape` 启动 job**：依赖转义正确性。
  改 argv 表（`jobstart({...})`）可消除整类风险，但 `-w` 输出重定向
  （`1> file`）需要 shell，动它要连着重定向方式一起设计。
- **`outline.ts_collect_items` 的 `loop is executed at most once`**：循环体
   末尾显式 `break`，"取第一条"是有意行为（已加注释），保留。
- **`import_swagger` 刻意忽略 `spec.schemes/host`**（生成 URL 一律
  `{{base_url}}`）：与 OpenAPI 导入器行为一致，已在代码注释中写明；若未来
  想保留原始主机，两处要一起改。
